# WiFiGraph 特殊场景与设计注意事项

日期：2026-08-11  
状态：草案

## 1. 百万级代码仓

WiFi MAC 代码仓达到百万级规模，不能假设可以把大量源码、调用关系或 Target 信息一次性交给 Agent。

设计要求：

- 查询必须支持 limit / 分页；
- 默认只返回摘要；
- 源码按函数或指定行范围读取；
- 不一次返回完整 Target 宏环境；
- Metadata 索引优先支持按函数名、Feature、Flow、Event、Target 快速过滤；
- Joern CPG 按 Target 独立保存，不做全仓跨 Target 大图合并。

系统目标不是一次理解整个代码仓，而是帮助 Agent 按当前问题逐步导航。

---

## 2. 多芯片、多 Target

同一份代码可能服务多个芯片、产品和编译 Target。

因此不能把：

```text
chip
host/device
product
```

固定成有限枚举。

Target 必须是开放标识，芯片、运行侧等作为 Metadata 维度存在。

同一个源码函数可能存在于：

```text
chip7-host
chip8-host
chip9-host
```

也可能只存在于某一个 Target。

领域索引应明确记录：

```text
Function
    ↓
在哪些 Target 实际存在
```

Agent 根据当前问题选择正确 Target 后，再进入对应 Joern CPG。

---

## 3. Host / Device 分离

Host 和 Device 属于不同编译环境，通常对应不同 CPG。

Joern 无法天然把：

```text
Host function
→ Event
→ Device handler
```

识别成一条完整调用链。

因此 Metadata 需要保存 Event 相关信息，例如：

```text
event name
producer
consumer
side
target
source location
```

WiFiGraph 只负责提供跨侧导航信息。

是否沿 Event 继续分析，以及选择哪一个 Device / Host CPG，由 Agent 决定。

---

## 4. 公共代码

Host 或 Device 中可能存在大量多芯片共享代码。

公共函数不能因为当前查询 Target 是 chip8，就被永久标记为：

```text
chip8 function
```

应区分：

```text
源码函数
```

和：

```text
该函数在哪些 Target 中实际参与编译
```

避免把公共代码错误归属到单一芯片。

---

## 5. OPS / Hook / Function Pointer

芯片差异经常通过：

```c
ops->xxx()
hook()
callback()
function pointer
```

实现。

这类调用不能简单按照普通直接调用关系处理。

V1 不要求 Metadata 层自动解析最终实现。

Metadata 只需要标记：

```text
hook_related
hook symbol
source location
相关 Target
```

Agent进入具体 Target 的 Joern CPG 后，再结合初始化代码、ops table 和关键源码判断真实实现。

系统不得因为存在多个可能实现就自动选择其中一个并作为确定事实返回。

---

## 6. 大量宏定义

单个 Target 可能存在 200+ 宏。

完整宏环境属于 Target Profile，不属于每一个函数。

函数只记录与自身有关的条件，例如：

```text
FEATURE_TX_OFFLOAD
CHIP_VERSION >= 8
```

Agent查询函数时只返回相关宏。

需要更多信息时再显式查询 Target 中具体宏值。

避免：

```text
每查询一个函数
→ 返回数百个宏
```

导致上下文快速膨胀。

---

## 7. 条件编译

同一份源码在不同 Target 中可能启用完全不同的代码。

因此 Metadata 中看到某个函数，不代表它一定存在于当前 Target。

必须结合：

```text
FunctionTarget
```

确认函数是否真实出现在当前 CPG 中。

源码扫描得到的：

```text
#if / #ifdef
```

信息主要用于解释和导航。

Joern CPG 中真实存在的代码才代表当前 Target 的有效编译视角。

---

## 8. Flow 分类

“所属流程”主要用于帮助 Agent 快速筛选代码，例如：

```text
TX
RX
SCAN
ROAM
DATA_TX
TX_COMPLETE
```

V1 只做轻量分类。

可以根据：

```text
函数名
文件名
目录
模块命名
```

自动推断。

推断结果必须标记为 inferred，并支持人工修改。

Flow 不能被当成真实执行路径。

例如：

```text
flow = DATA_TX
```

只表示：

> 这个函数大概率与 DATA_TX 相关。

不表示系统已经证明它位于某条完整 DATA_TX 调用链中。

---

## 9. Feature 分类

一个函数可能同时属于多个 Feature，例如：

```text
TX
TX_OFFLOAD
MLO
POWER_SAVE
```

Feature 应采用多值标签，而不是单一分类。

Feature 可以来自：

```text
宏
目录
命名规则
人工维护
```

允许后续人工纠正。

---

## 10. Event 信息不一定唯一

同一个 Event 可能：

- 有多个 producer；
- 有多个 consumer；
- 不同芯片对应不同 handler；
- 不同 Target 下注册关系不同。

因此 Event 查询应返回候选集合和 Target 信息。

不能默认：

```text
Event X
→ 唯一 Function Y
```

Agent结合当前芯片和运行环境继续判断。

---

## 11. 同名函数

大型 C/C++ 代码仓中可能存在多个同名函数。

函数不能只以：

```text
name
```

作为唯一标识。

至少需要结合：

```text
repository
file
function name
signature / occurrence
```

查询结果不唯一时必须返回候选，不自动选择。

---

## 12. 日志定位

问题定位经常从：

```text
某条打印
文件:行号
某个函数
```

开始。

Metadata 和源码查询必须支持：

```text
函数名搜索
文件 + 行号定位
日志字符串搜索
```

定位之后再交给 Agent 选择相应 Target 和 Joern CPG。

---

## 13. Metadata 不要求绝对准确

领域 Metadata 的目标是导航，不是替代源码和 Joern。

建议区分来源：

```text
verified
inferred
manual
```

例如：

```text
Target occurrence → verified
宏条件 → source verified
Flow → inferred
人工 Feature → manual
```

Agent可以知道哪些信息可以直接作为事实，哪些只是下一步查询提示。

---

## 14. 人工修正必须保留

对于：

```text
Flow
Feature
Event
Side
领域标签
```

自动推断可能存在错误。

因此系统必须允许人工修正。

人工修正应独立保存，重新索引不能覆盖。

这样系统可以随着真实使用逐步积累更加准确的领域知识，而不要求 V1 一开始自动理解整个驱动。

---

## 15. Metadata 与 Joern 的职责边界

这是整个系统最重要的原则。

Metadata 回答：

```text
这个函数是什么？
大概属于什么？
在哪些 Target？
运行在哪一侧？
与哪些宏、Feature、Event 有关？
下一步可能应该看哪里？
```

Joern 回答：

```text
在这个具体 Target 下：
谁调用它？
它调用谁？
有没有路径？
控制流是什么？
数据如何传播？
```

Agent负责：

```text
当前问题应该查哪个 Target？
下一步应该查询哪个函数？
是否需要跨 Host / Device？
这些代码事实说明了什么？
```

任何新功能设计时，都应该先判断它属于哪一层，避免把 Agent Workflow 逐步下沉到 WiFiGraph 中。

---

## 16. 核心原则总结

面对复杂 WiFi MAC 代码仓，WiFiGraph 应坚持：

```text
不试图一次理解整个代码仓
不试图提前生成完整运行流程
不自动解决所有 Hook
不合并所有 Target CPG
不把全部宏塞给 Agent
不把推断结果冒充事实
```

而是提供：

```text
轻量 Metadata
+
准确 Target 定位
+
按需 Joern 查询
+
源码证据
+
人工可纠正的领域标签
```

让 Agent 在具体问题下逐步完成真正的工程分析。
